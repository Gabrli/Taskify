import { ReactNode } from "react"
import { IconType } from "react-icons"
interface landingFooterElList{
    id:number,
    title:string,
    icon: ReactNode | null,
    link:string,

}

export type {landingFooterElList}