import NotyfiactionWrapper from "../notyfiactionComponents/notyfiactionWrapper";

export default function NotyfiactionPage(){
    return (
        <main className="notyfication_page w-full h-screen flex justify-center items-center">
            <NotyfiactionWrapper/>
        </main>
    )
}